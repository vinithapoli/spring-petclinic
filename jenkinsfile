pipeline {
    agent any

    stages {
        stage('Git clone') {
            steps {
                git branch: 'main', url: 'https://github.com/vinithapoli/spring-petclinic.git'
            }
        }
         stage('generate build') {
            steps {
                sh 'mvn package'
            }
        }
    }
}
